{% extends 'base_login.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}
    <div class="form-login">
      <div style="color: white">
          <p>
              Si l'email saisie correspond a un compte existant, un email vous a été envoyer contenant un lien que vous pouvez utiliser pour changer votre mot de passe.
              Ce lien expira dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
          </p>
          <p>Si vous n'avez pas recus d'email veuillez vérifier dans votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}">réessayer</a>.</p>
      </div>
    </div>
{% endblock %}
