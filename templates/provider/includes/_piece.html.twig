{% extends app.request.query.get('isAjax', false) ? 'layout_empty.html.twig' : 'provider/show.html.twig' %}

{% block content %}
    <form id="filters">
        <input type="hidden" name="provider" data-field="provider.id" data-condition="=" value="{{ provider.id }}">
    </form>

    <table id="table-stock" class="datatable" data-url="{{ path('stock_paginate') }}" data-filters="#filters">
        <thead>
        <tr>
            <th>Référence</th>
            <th>Nom</th>
            <th>Prix</th>
            <th>Quantité</th>
            <th>Services</th>
            <th>Fournisseur</th>
            <th>Détails</th>
        </tr>
        </thead>
    </table>
{% endblock %}
